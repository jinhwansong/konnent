name: Deploy Backend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Deploy to Vultr
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.VULTR_HOST }}
          username: root
          key: ${{ secrets.VULTR_SSH_KEY }}
          port: 22

          script: |
            set -e
            cd /var/www/konnect-back
            
            echo "📥 최신 코드 가져오기..."
            git fetch origin
            git reset --hard origin/main

            echo "📦 패키지 설치 중..."
            export NODE_ENV=production
            
            if [ -f "package-lock.json" ]; then
              echo "npm ci --omit=dev 실행"
              npm ci --omit=dev --no-audit --prefer-offline
            else
              echo "npm install --omit=dev 실행"
              npm install --omit=dev --no-audit --prefer-offline
            fi

            echo "🏗️ NestJS 빌드 중..."
            npm run build

            echo "🚀 PM2 재시작..."
            # 기존 프로세스 있으면 restart, 없으면 start
            pm2 restart konnect-back || pm2 start dist/main.js --name konnect-back

            echo "🧹 PM2 리스트 저장..."
            pm2 save

            echo "✅ 배포 완료!"
